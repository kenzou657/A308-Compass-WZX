/*
 * 应用层 - 业务逻辑
 * 
 * 本目录包含所有应用模块，实现业务逻辑和控制算法。
 * 应用层调用驱动层接口，不直接访问硬件。
 * 
 * 模块列表:
 * - pid_controller.c/h    : PID 算法实现
 * - line_tracker.c/h      : 循迹控制逻辑
 * - digit_recognizer.c/h  : 数字识别解析
 * - transport_fsm.c/h     : 物品搬运状态机
 * - navigator.c/h         : 路径规划、导航
 * 
 * 设计原则:
 * 1. 应用层不直接访问硬件
 * 2. 应用层通过驱动层接口与硬件交互
 * 3. 应用层不在 ISR 中执行，仅在主循环中执行
 * 4. 应用层可使用浮点运算（在主循环中）
 */
