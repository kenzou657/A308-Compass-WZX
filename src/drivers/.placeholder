/*
 * 驱动层 - 硬件抽象
 * 
 * 本目录包含所有硬件驱动模块，提供统一的硬件接口。
 * 驱动层不包含业务逻辑，仅负责硬件操作。
 * 
 * 模块列表:
 * - motor_driver.c/h      : PWM 电机控制
 * - servo_driver.c/h      : 50Hz 舵机控制
 * - uart_driver.c/h       : 中断接收、帧验证
 * - line_sensor.c/h       : 循迹传感器采样
 * - imu_sensor.c/h        : IMU 数据解析
 * - gpio_driver.c/h       : GPIO 初始化、控制
 * 
 * 设计原则:
 * 1. 每个驱动模块有明确的接口 (.h 文件)
 * 2. 驱动层不调用应用层函数
 * 3. 驱动层可被多个应用模块共享
 * 4. 使用 DriverLib 函数，禁止直接寄存器操作
 */
