/*
 * 中断服务函数 - ISR 集中管理
 * 
 * 本目录包含所有中断服务函数的实现。
 * ISR 应该保持最小化，仅设置标志位或更新计数器。
 * 
 * 模块列表:
 * - isr_handlers.c/h      : 所有 ISR 集中管理
 * 
 * 设计原则:
 * 1. ISR 仅设置标志位或更新计数器
 * 2. ISR 不执行复杂的算法或函数调用
 * 3. ISR 不使用浮点运算
 * 4. ISR 应该尽可能快地返回
 * 
 * ISR 标志位:
 * - g_timer_flag          : 定时器中断标志
 * - g_uart_frame_ready    : UART 帧接收完成标志
 * - g_sensor_data_ready   : 传感器数据就绪标志
 */
